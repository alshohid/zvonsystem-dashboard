import RoleSectionPage from "@/src/components/design/RoleSectionPage";

export default function AdminReportsPage() {
  return (
    <RoleSectionPage
      eyebrow="Admin"
      title="Reports"
      description="Reporting pages now sit inside the admin dashboard role instead of the previous scattered page structure."
    />
  );
}
